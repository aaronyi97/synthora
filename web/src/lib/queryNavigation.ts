export function buildSocraticPath(_question: string, basePath = "/socratic/new"): string {
  return basePath;
}

export function buildRoundtablePath(_question: string, basePath = "/roundtable"): string {
  return basePath;
}
