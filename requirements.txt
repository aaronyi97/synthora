# Synthora - Multi-model AI Orchestration System
# Pin exact versions for reproducibility

fastapi==0.128.8
starlette==0.52.1
python-multipart==0.0.22  # RC-11: CVE-2026-24486 fixed in 0.0.22
cryptography==46.0.5  # CVE-2026-26007 fixed; alibabacloud-tea-openapi declares <45.0.0 but runtime-compatible
uvicorn==0.34.0
uvloop>=0.17.0           # required by uvicorn workers; not pinned = latest breaks EventLoopPolicy
websockets>=10.0,<12     # uvicorn websockets_impl uses websockets.legacy which was removed in v12+
httptools==0.6.1         # newer versions break uvicorn httptools_impl.HttpRequestParser (confirmed broken on Linux/Python3.12)
httpx==0.27.2
httpx[socks]
openai==1.58.1
pyyaml==6.0.2
python-dotenv==1.0.1
sentry-sdk[fastapi]==2.55.0
sse-starlette==2.1.3
aiosqlite==0.22.1
bcrypt==4.2.1
python-magic==0.4.27
rich==13.9.4
click==8.1.7
tavily-python==0.7.21

# Optional: Aliyun SMS (phone registration).
# Install separately: pip install alibabacloud-dysmsapi20170525==4.5.0 alibabacloud-tea-openapi==0.4.3
# Not in base requirements because alibabacloud-tea-openapi pins cryptography<45.0.0
# which conflicts with cryptography>=46.0.5 (CVE-2026-26007 fix).
# The SMS adapter degrades gracefully when these packages are absent.
# alibabacloud-dysmsapi20170525==4.5.0
# alibabacloud-tea-openapi==0.4.3

# Dev dependencies
pytest==8.3.4
pytest-asyncio==0.24.0
pytest-cov==5.0.0
